#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  // ----------- WHILE LOOPS ------------
  
  // Method 1: while(true)
  // while(true) {
  //   cout << "Infinite loop using while(true)" << endl;
  // }

  // Method 2: while(1)
  // while(1) {
  //   cout << "Infinite loop using while(1)" << endl;
  // }

  // Method 3: while with a non-zero constant
  // while(99) {
  //   cout << "Infinite loop using while(99)" << endl;
  // }

  // ----------- FOR LOOPS ------------

  // Method 1: for(;;)
  // for(;;) {
  //   cout << "Infinite loop using for(;;)" << endl;
  // }

  // Method 2: for with always-true condition
  // for(int i = 0; i < 1 || i == 0; ) {
  //   cout << "Infinite loop using tricky condition" << endl;
  // }

  return 0;
}
